#pragma once

#include <algorithm>
#include "world.h"

class Structures {
public:
    static Structure makeTree(World world, int x, int y);
    static Structure makeTree1(World world, int x, int y);
};
